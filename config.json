{
  "name": "ssh",
  "viewName": "SSH",
  "type": "executer",
  "main": "app.js",
  "execProgram": "node",
  "exposeRoute": false,
  "imgUrl": "logo.png",
  "version": "1.0.0",
  "description": "Kaholo Plugin with SSH utilities, such as remote command execution and secure copy.",
  "category": "Basic Tools",
  "keywords": [
    "ssh"
  ],
  "auth": {
    "authId": "sshAccount",
    "params": [
      {
        "name": "hostname",
        "viewName": "Hostname",
        "type": "string",
        "description": "Hostname or IP address.",
        "placeholder": "192.168.10.7",
        "required": true
      },
      {
        "name": "username",
        "viewName": "Username",
        "type": "string",
        "description": "Username to authenticate with the host.",
        "placeholder": "root",
        "required": true
      },
      {
        "name": "password",
        "viewName": "Password",
        "type": "vault",
        "description": "Password to authenticate with the host. In case both SSH Key and Password parameters are provided, the SSH Key has priority."
      },
      {
        "name": "privateKey",
        "viewName": "SSH Key",
        "type": "vault",
        "description": "Key to authenticate with the host. This parameter accepts both the SSH key file content and a path to the SSH key file on the agent.",
        "placeholder": "-----BEGIN OPENSSH PRIVATE KEY-----"
      },
      {
        "name": "privateKeyPassphrase",
        "viewName": "SSH Key Passphrase",
        "type": "vault",
        "description": "Passphrase protecting the private key, set during key generation. Empty by default."
      },
      {
        "name": "port",
        "viewName": "Port",
        "type": "int",
        "description": "The port used to connect with SSH to the remote host.",
        "default": 22
      }
    ]
  },
  "methods": [
    {
      "name": "executeCommand",
      "viewName": "Execute Command",
      "params": [
        {
          "name": "command",
          "viewName": "Command",
          "type": "text",
          "description": "The command to execute on the remote host.",
          "placeholder": "uptime",
          "required": true
        }
      ]
    }
  ]
}
